{
  "name": "@veil/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VEIL business payments client + CLI (shield / pay / audit)",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "veil": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ethers": "^6.13.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
